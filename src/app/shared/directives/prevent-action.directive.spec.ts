import { PreventActionDirective } from './prevent-action.directive';

describe('PreventActionDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventActionDirective();
    expect(directive).toBeTruthy();
  });
});
